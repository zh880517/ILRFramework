Editor :编辑器扩展代码
Hitfix :游戏核心逻辑代码，热更新用
ILUtils : 核心热更新模块，封装不同的模式，以及热更新模块的加载处理
ILBinding ：ILRuntime的绑定代码，代码为自动生成
Framework: 框架层，不需要放到热更新，对性能有一定要求的模块，根据不同的功能划分子文件夹
MainModule ：游戏主模块，主要是启动热更新相关，不能被Editor外别的模块引用，只要是被外部模块访问的代码都不可以放这里面
